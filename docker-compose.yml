services:
  backend:
    build: ./backend
    container_name: cdc-backend
    environment:
      - DB_HOST=10.0.1.90
      - DB_USER=sa
      - DB_PASSWORD=Rfx14w.14w.
      - DB_DATABASE=hospital
    ports:
      - "3001:3001"
